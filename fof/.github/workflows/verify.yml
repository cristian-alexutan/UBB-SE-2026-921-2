name: Verify Build & Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup .NET 10
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Restore Dependencies
      run: dotnet restore TicketSellingModule.slnx

    - name: Build Solution
      run: dotnet build TicketSellingModule.slnx -c Debug --no-restore /p:Platform=x64 /p:PublishReadyToRun=false

    - name: Run Unit Tests
      run: dotnet test TicketSellingModule.slnx -c Debug --no-build --verbosity normal /p:Platform=x64