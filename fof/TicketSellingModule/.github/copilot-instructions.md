# Copilot Instructions

## Project Guidelines
- User prefers not to use wrapper grouping classes like EmployeeRoleGroups and prefers simpler direct service methods instead.
- User prefers to move business logic out of ViewModels into services, keeping ViewModels thin. Minimize validation and data transformation logic in ViewModels; have service methods handle these responsibilities.