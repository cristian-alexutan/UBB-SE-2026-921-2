<?xml version="1.0" encoding="utf-8"?>

<UserControl
    x:Class="TicketSellingModule.WinUI.AirportAdmin.Components.FlightListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Foreground="#F8FAFC">

    <Border BorderBrush="#D0D0D0"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Flight No." FontWeight="Bold"/>
                <TextBlock Grid.Column="1" Text="Date" FontWeight="Bold"/>
                <TextBlock Grid.Column="2" Text="Destination" FontWeight="Bold"/>
                <TextBlock Grid.Column="3" Text="Runway" FontWeight="Bold"/>
                <TextBlock Grid.Column="4" Text="Gate" FontWeight="Bold"/>
                <TextBlock Grid.Column="5" Text="Assigned Crew" FontWeight="Bold"/>
            </Grid>

            <ListView x:Name="FlightsListView"
                      Grid.Row="1"
                      SelectionMode="Single"
                      ItemsSource="{x:Bind Items, Mode=OneWay}"
                      SelectedItem="{x:Bind SelectedItem, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="200"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding FlightNumber}"/>
                            <TextBlock Grid.Column="1" Text="{Binding DateText}"/>
                            <TextBlock Grid.Column="2" Text="{Binding DestinationText}"/>
                            <TextBlock Grid.Column="3" Text="{Binding RunwayText}"/>
                            <TextBlock Grid.Column="4" Text="{Binding GateText}"/>
                            <TextBlock Grid.Column="5" Text="{Binding CrewText}" TextWrapping="Wrap"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>
</UserControl>
