<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="TicketSellingModule.WinUI.AirportAdmin.Components.AirportSectionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border BorderBrush="#D0D0D0"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12">

        <StackPanel Spacing="10">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{x:Bind Title, Mode=OneWay}"
                           FontSize="18"
                           FontWeight="SemiBold"/>

                <Button Grid.Column="1"
                        Content="Add"
                        Command="{x:Bind AddCommand, Mode=OneWay}" />
                <Button Grid.Column="3"
                        Content="Edit"
                        Command="{x:Bind EditCommand, Mode=OneWay}" />
                <Button Grid.Column="5"
                        Content="Delete"
                        Command="{x:Bind DeleteCommand, Mode=OneWay}" />
            </Grid>

            <ListView x:Name="ListViewControl"
                      SelectionMode="Single"
                      SelectedItem="{x:Bind SelectedItem, Mode=TwoWay}">
                <ListView>
                    <DataTemplate>
                        <StackPanel Padding="0,6">
                            <TextBlock Text="{Binding}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView>
            </ListView>

        </StackPanel>
    </Border>
</UserControl>
