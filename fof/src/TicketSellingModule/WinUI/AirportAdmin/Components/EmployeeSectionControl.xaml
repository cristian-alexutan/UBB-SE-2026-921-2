<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="TicketSellingModule.WinUI.AirportAdmin.Components.EmployeeSectionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border BorderBrush="#D0D0D0"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12">
        <StackPanel Spacing="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{x:Bind SectionTitle, Mode=OneWay}"
                           FontSize="18"
                           FontWeight="SemiBold"/>

                <Button Grid.Column="1" Content="Add" Command="{x:Bind AddCommand}" CommandParameter="{x:Bind SectionTitle}"/>
                <Button Grid.Column="3" Content="Edit" Command="{x:Bind EditCommand}" CommandParameter="{Binding ElementName=EmployeeListView, Path=SelectedItem}"/>
                <Button Grid.Column="5" Content="Delete" Command="{x:Bind DeleteCommand}" CommandParameter="{Binding ElementName=EmployeeListView, Path=SelectedItem}"/>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Id" FontWeight="Bold"/>
                <TextBlock Grid.Column="1" Text="Name" FontWeight="Bold"/>
                <TextBlock Grid.Column="2" Text="Role" FontWeight="Bold"/>
                <TextBlock Grid.Column="3" Text="Salary" FontWeight="Bold"/>
            </Grid>

            <ListView x:Name="EmployeeListView"
                      SelectionMode="Single"
                      ItemsSource="{x:Bind ItemsSource, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="120"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding Id}"/>
                            <TextBlock Grid.Column="1" Text="{Binding Name}"/>
                            <TextBlock Grid.Column="2" Text="{Binding Role}"/>
                            <TextBlock Grid.Column="3" Text="{Binding Salary}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Border>
</UserControl>
