<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TicketSellingModule.WinUI.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#0F172A">

    <Page.Resources>
        <SolidColorBrush x:Key="CustomAccentBrush" Color="#2bb8c0"/>
        
        <Style x:Key="DashboardButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="260"/>
            <Setter Property="Height" Value="140"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Page.Resources>

    <Grid Padding="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="1">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="20">

                <TextBlock Text="Welcome to the Flight Module!"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>

                <TextBlock Text="What type of user are you?"
                           FontSize="16"
                           Foreground="#B0B0B0"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,25"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    
                    <StackPanel.Resources>
                        <ResourceDictionary>
                            <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#1E293B"/> 
                            <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="White"/> 
                            <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="#2bb8c0"/> 
                            <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#0F172A"/>
                            <SolidColorBrush x:Key="ButtonForegroundPressed" Color="White"/>
                            <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="#2bb8c0"/>
                        </ResourceDictionary>
                    </StackPanel.Resources>

                    <Button Style="{StaticResource DashboardButtonStyle}" Command="{x:Bind ViewModel.NavigateToCompanyCommand}">
                        <TextBlock Text="Company Representative" TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button>
                    <Button Content="Airport Administrator"
                            Style="{StaticResource DashboardButtonStyle}"
                            Command="{x:Bind ViewModel.NavigateToAdminCommand}" />
                    <Button Content="Airport Staff Member"
                            Style="{StaticResource DashboardButtonStyle}"
                            Command="{x:Bind ViewModel.NavigateToStaffCommand}" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Page>